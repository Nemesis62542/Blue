# Entity Authoring

生物エンティティ（ローポリ + 揺れもの）のモデル / リグ / アニメーション制作規約。
ランタイム側の実装は `Assets/Scripts/Entity/Common/` を参照。

## 役割分担

一番重要な約束。**同じボーンをアニメーションと ProceduralBoneChain の両方で動かさない。**

| 担当 | 動かすもの |
|------|-----------|
| アニメーション（Animator） | ルートボーンの向き・その場の揺れ／顎・エラなど独立パーツ |
| `ProceduralBoneChain` | 胴体のうねり（ルートに遅れて追従）／ヒレの慣性 |
| コード（`BaseSwimmer` 等） | ワールド空間の移動・旋回 |

胴体のうねりをアニメーションに焼き込むと、Chain がそれをローパスフィルタとして打ち消す。
うねりは Chain に任せ、アニメーションは**ルートを振るだけ**にする。

## 座標系と向き

| 項目 | 値 |
|------|-----|
| Unity での正面 | +Z |
| Unity での上 | +Y |
| Blockbench / Blender での正面 | −Z（書き出し時に +Z へ変換される） |
| 左右 | モデル自身から見た向き。`*_Right` が Unity の +X 側 |

書き出し後に Unity で 1 度だけ確認する。前後が逆だと移動コードが全部裏返る。

## ボーン階層

```
Root(Head) ─ Neck ─ Body_Front ─ Body_Back ─ Spine_Front ─ Spine_Back ─ Tail ─ Tail_end
   │                    │             │
   └ Jaw ─ Jaw_end      │             └ Fin_Back_L/R ─ _end
                        └ Fin_Front_L/R ─ _end
```

- **ルートボーン 1 本から始まる単一の連続チェーン**にする。頭と胴を兄弟にしない（親子関係が無いと Chain が追従を作れない）
- 揺れもの（ヒレ・触手）は必ずチェーン上のボーンから分岐させる
- 揺れものは **2 ボーン以上**。`BoneChainBranch` は「自分から子への方向」で向きを決めるので、`_end` が無い 1 ボーンのヒレは制御できない
- 軸反転だけのゼロ長ボーン（`Neck` のような）は置いてもよいが、`ProceduralBoneChain.segments` には入れない。ボーン長 0 になり方向が求まらない

### 命名

| 種類 | 規則 | 例 |
|------|------|-----|
| チェーン | 前から後ろへ通し番号 | `Spine_01` … `Spine_04` |
| 揺れもの | `<部位>_<L\|R>` | `Fin_Front_L` |
| 末端 | 親名 + `_end` | `Tail_end` |

`Front` / `Back` が階層順と食い違う命名（`Spine_Back` が `Body_Back` より後ろ、など）は避ける。

## レストポーズ

- **完全にまっすぐ**にする。軸変換と意図的なオフセット（顎の開き等）以外、ローカル回転は 0
- 次の 3 つが一致していること
  1. FBX のノード transform
  2. Idle クリップの 1 フレーム目
  3. Blender / Blockbench の Rest Position
- 書き出し前に必ず Rest Position に戻す。ポーズ中に書き出すとノード transform だけがポーズを持ち、Unity のプレハブ既定姿勢が曲がる

まっすぐなレストポーズは `ProceduralBoneChain` の復元先そのものなので、ここが曲がっていると挙動が全部ずれる。

## アニメーション

- 胴体のうねりは入れない（上の役割分担）
- ルートボーンの `translation` は**その場の揺れだけ**。前進を入れるとコード側の移動と二重になる
- `ProceduralBoneChain` が制御するボーンには回転カーブを付けない
- クリップ長を必ず確認する。Blockbench はアニメーション長を設定しないと数フレームで書き出される
- ループするクリップは先頭と末尾のキーを一致させる

## 書き出し

- **FBX を正とする**。glTF は確認用。両方を Unity に入れない（フィン角度やボーン構成が食い違う）
- 書き出し前に不要オブジェクトを削除する（作業用のプリミティブ等）
- メッシュノードの回転・スケールは 0 / 1 にする
- `_end` ボーンを含めて書き出す

### ModelImporter 設定

| 項目 | 値 | 理由 |
|------|-----|------|
| Animation Type | Generic | ヒューマノイドではない |
| Optimize Game Objects | オフ | ボーン Transform を Chain から参照する |
| Import BlendShapes | オフ | 使わない |
| Import Cameras / Lights | オフ | 使わない |

## ProceduralBoneChain の設定

| 項目 | 値 | 理由 |
|------|-----|------|
| `useAnimatorRotation` | **false** | 対象ボーンにアニメカーブが無い構成なので、true だと自分が前フレームに書いた値を読み続けて姿勢が戻らない |
| `segments[0]` | うねりを始めたい 1 つ手前のボーン | 先頭は制御されず「先導役」として読まれるだけ。`lagTime` は無視される |
| `segments[1..]` | 前から後ろの順 | 配列順 = 親子順であること |
| `lagTime` | 頭側 0.06 秒 → 末端 0.25 秒 | 親から遅れて付いてくる時間。**秒単位でそのまま効く** |
| `lagFalloff` | 0 | 末端ほど `lagTime` を伸ばす倍率。セグメント個別に入れるなら 0 のままでよい |
| `overshoot` | 0.5〜0.7 | 0 だと目標を行き過ぎないので動きが硬くなる。揺れが欲しいなら上げる |
| `maxBendAngle` | 45〜60 | レストポーズからの逸脱角。まっすぐなレスト前提 |

### 遅れと揺れの決め方

追従はバネ・ダンパ（二次系）。パラメータは次のように対応する。

```
ωn = 1 / (lagTime × (1 + 末端度 × lagFalloff))     固有角振動数
ζ  = 1 - overshoot                                  減衰比
```

| `overshoot` | ζ | 挙動 |
|------------|-----|------|
| 0 | 1.0 | 臨界減衰。行き過ぎずに収束する（硬い） |
| 0.5 | 0.5 | 軽く揺れ戻る |
| 0.7 | 0.3 | はっきり揺れる。駆動周波数が ωn に近いと振幅が増える |

`overshoot` を 0 にすると一次の指数補間とほぼ同じで、**目標を行き過ぎないため「揺れ」にはならない**。
アニメーションでうねりを与えていない構成では、ここを上げないと胴体が硬いままになる。

`lagTime` は 0.01〜1 秒でクランプされる。1 秒に張り付くと事実上止まって見えるので、
末端でも 0.3 秒程度までに収めるのが目安。

### 揺れもの（分岐）の設定

ヒレや触手は `ProceduralBoneChain` の `branches` に、**1 本 = 1 要素**で追加する。

| 項目 | 設定 |
|------|------|
| `branchName` | 任意のラベル。`Fin_Front_L` など |
| `branchBones` | Size 2 以上。[0] が根元、最後が `_end` |
| `parentSegmentIndex` | そのヒレが生えているボーンの、`segments` 内での要素番号 |
| `lagTime` | 0.08〜0.15 秒。胴体より短くすると軽く見える |
| `overshoot` | 0.6〜0.8。ヒレは胴体より揺らしてよい |
| `followStrength` | 0.1〜0.3 |

押さえておく点：

- **末端の `_end` を必ず含める。** 最後の要素は「一つ前のボーンが向くべき方向」の基準にしか
  使わず回転しないので、1 本だけ入れても何も起きない
- ヒレが実際に追従する相手は Unity の**階層上の親**。`parentSegmentIndex` は慣性用に
  親セグメントの速度を取るためだけに使うので、多少ずれても破綻はしない
- `followStrength` は親の**移動速度**に比例する。前進速度が一定だとヒレが後ろへ寝たままになるので、
  泳ぎながら揺らしたいなら小さめにして、揺れは `overshoot` 側で作る
- **アニメーションカーブが付いているボーンを分岐に入れない。** 分岐は初期姿勢基準で
  毎フレーム上書きするので、アニメが打ち消される（顎など動かすものはアニメ側に任せる）

分岐は `segments` と違い、`useAnimatorRotation` の影響を受けず常に初期姿勢を基準にする。
アニメを持たないヒレでも、倒れた分がまっすぐへ戻ってくる。

## モデルを差し替える

**原則：アセットを削除してから入れ直さない。ファイルだけを上書きする。**

削除すると `.meta` が消え、参照もインポート設定もマテリアルも同時に飛ぶ。
上書きなら `.meta` が据え置かれるので、Unity は既存の設定のまま再インポートしてくれる。
作業量が変わるのは、**リグの構造が変わるかどうか**だけ。

### ケース A：構造が変わらない（メッシュ・UV・テクスチャ・アニメの修正だけ）

FBX を**同じパス・同じファイル名で上書き**する。`.meta` はそのまま残す。

- guid が変わらないので、プレハブ・マテリアル割り当て・Animator・EntityData の参照が全部生きる
- ボーン参照も、ボーン名と階層パスが変わっていなければ維持される
- Blockbench / Blender の書き出し先を `Assets/Model/Entity/<名前>/<名前>.fbx` に固定しておくと、
  書き出すだけで更新が完了する

`.meta` を一緒に上書きしたり削除したりしないこと。guid が変わって全参照が切れる。

#### なぜ「削除→インポート」ではだめか

アセットを削除すると `.meta` も一緒に消える。`.meta` には次が入っている。

| `.meta` の中身 | 消えると何が起きるか |
|---------------|--------------------|
| `guid` | プレハブ・EntityData からの参照が全部切れる |
| `ModelImporter` 設定 | Rig / Scale / Animation Type / Normals などが既定値に戻る |
| `externalObjects` | マテリアルの割り当てが外れて、FBX 内蔵マテリアルに戻る |
| `clipAnimations` | クリップの分割・ループ設定が消える。**`internalID` も再生成されるので AnimatorController の参照が切れる** |

つまり「削除してから入れ直す」と、**参照・インポート設定・マテリアル・アニメ設定が同時に飛ぶ**。
ファイルだけを上書きすれば `.meta` は据え置かれ、Unity は既存の設定でそのまま再インポートする。

クリップ設定は `takeName` で対応付けられるので、アニメーション名を変えなければ維持される。
新しく増えたクリップだけ Inspector の Clips に追加すればよい。

### ケース B：構造が変わる（ボーンの追加・削除・改名・階層変更）

**この場合も上書きする。削除しない。** 違うのは、上書き後に手で直す箇所があること。

FBX 内サブアセットの fileID は名前だけでなく**階層パスからも決まる**ので、同名のボーンでも
親が変われば別 ID になり、ボーンを直接指している参照は切れる。逆に言うと、切れるのはそこだけ。

上書きで維持されるもの / 切れるもの：

| | |
|---|---|
| 維持 | guid、ModelImporter 設定、マテリアル割り当て、クリップ設定と `internalID` |
| 維持 | モデルプレハブの**ルート** GameObject（fileID は固定）。ルートに付けたコンポーネントは残る |
| 切れる | ボーン Transform への参照（`ProceduralBoneChain.segments` など） |
| 切れる | ルート以外の子オブジェクトに付けたコンポーネント |

手順：

1. FBX を同じパスへ上書きする（`.meta` は残す）
2. Inspector の Clips に、増えたアニメーションを追加する
3. `ProceduralBoneChain.segments` にボーンを割り当て直す
4. Collider のサイズを新しいモデルに合わせる
5. 動作確認

コンポーネントは**なるべくルートに付ける**と、差し替え時に生き残る範囲が広くなる。
子オブジェクトに付ける必要がある場合は、差し替えのたびに付け直す前提で考える。

### 参照の経路

差し替えで触るのはここだけ。

```
Resources/EntityDataRegistry.asset
  └ ScriptableObjects/Entity/<名前>.asset   (EntityData)
      └ object: プレハブのルート GameObject   ← 差し替え点はここ 1 箇所
```

プレハブをシーンへ直接置くと差し替え箇所が増えるので、**生成は EntityData 経由に寄せる**。
この経路を守っている限り、モデルを何度作り直しても書き換えるのは 1 フィールドで済む。

### 命名

1 体につき 1 つの名前に統一する。フォルダ / FBX / プレハブ / マテリアル / テクスチャ /
AnimationController / EntityData / スクリプトのすべてで同じ表記にすること。
表記が割れると、差し替えのたびにどれが現行か分からなくなる。

## 書き出し前チェックリスト

- [ ] Rest Position に戻した
- [ ] レストポーズがまっすぐ
- [ ] ルートから末端まで親子が 1 本に繋がっている
- [ ] 揺れものに `_end` がある
- [ ] 不要オブジェクトを消した
- [ ] メッシュノードの回転が 0
- [ ] 各クリップの長さが意図どおり
- [ ] 胴体ボーンに回転カーブが付いていない
- [ ] Unity に入れて +Z を向いている
- [ ] `useAnimatorRotation = false` になっている
- [ ] `overshoot` が 0 のままになっていない
