# Stage Authoring

実マップ（ステージ）の制作手順と規約を記載します。
地形パイプラインのクラス構成は `Assets/Scripts/World/` と `Assets/Scripts/Editor/World/` を参照。

## 手で触るアセットは2つ

| アセット | 何を決めるか |
|---------|------------|
| `StageGeneratorSettings` | 地形の**形**。断面・起伏・リージョン・造作 |
| `StageRecipe` | **ベイクの入力**。レイアウト、ハイトマップ、スプラット(`biomes`)、散布 |

```
StageGeneratorSettings ──生成──> Source/*.exr, *.png ──┐
                                                      ├──ベイク──> Generated/
StageRecipe（レイアウト・biomes・scatter）─────────────┘
```

`Source/` と `Generated/` の中身は生成物なので手で編集しない。設定が2つに分かれているのは、外部ツールで作った画像を置く場合に `StageGeneratorSettings` が不要になるため。クラスの一覧は [ClassResponsibilities.md](ClassResponsibilities.md) を参照。

## 座標系の約束

全ステージで共通。ここを揃えておくと、散布フィルタもUI表示もステージ間で使い回せる。

| 項目 | 値 |
|------|-----|
| 水面 | Y = 0 |
| 水深 | -Y（水深250m → Y = -250） |
| ステージ中心 | 原点。1024m四方なので X/Z は -512 〜 +512 |
| `StageRecipe.waterLevel` | 0 固定 |

`ScatterLayer.depthRange` は水面からの水深(m)なので、この約束を守る限りステージをまたいでも同じ意味になる。

## ステージ構成

1ステージ = 1024m四方 / 4x4タイル（1枚256m）/ 1m per texel。
水平方向の構成は全ステージ共通で、変わるのは水深帯だけ。

| ステージ | 水深帯 | 海洋区分 | groupingId |
|---------|--------|---------|-----------|
| Stage01 | 0〜250m | 大陸棚・有光層 | 1 |
| Stage02 | 250〜1000m | 中深層（トワイライトゾーン） | 2 |
| Stage03 | 1000〜4000m | 漸深層 | 3 |
| Stage04 | 4000〜6000m | 深海層（深海平原） | 4 |
| Stage05 | 6000〜10000m | 超深海層（海溝） | 5 |

`groupingId` は Terrain の自動接続グループ。同じ値だと別ステージの Terrain 同士が接続を試みるため、必ずステージごとに変える。

### minHeight / maxHeight は水深帯のラベルではない

レイアウトの `minHeight` / `maxHeight` は **そのステージの地形が実際に占める高さの範囲**であって、水深帯の境界そのままではない。

Stage01（水深0〜250m）の場合:

```
maxHeight = +20m   … 水面から突き出す岩礁のぶんの余白
minHeight = -280m  … 最深部(-250m)の下に、Digger で洞窟を掘る余地を30m
HeightRange = 300m
```

深いステージほど、水深帯をそのままレンジにすると精度を捨てることになる。
たとえば Stage05 を Y -10030〜-5980（レンジ4050m）にすると、16bitハイトマップの高さ刻みが6cmになり、平坦な海底がテラス状の段差になる。
**海底が実際に起伏する範囲だけを囲う**こと。海溝の底が9500〜9900mに収まるなら `minHeight = -9950, maxHeight = -9400` とし、そこに至る斜面はステージ境界の演出で処理する。

目安として、HeightRange は 1000m を超えたあたりから16bitでは苦しくなる。超える場合はハイトマップを32bit float（EXR）で書き出す。

## フォルダ規約

```
Assets/Stages/
└── Stage01/
    ├── Stage01_Recipe.asset          # 正本。コミット対象
    ├── Source/                       # 外部ツールの書き出し。正本。コミット対象
    │   ├── Stage01_Height.png        # ハイトマップ（16bit）
    │   ├── Stage01_Mask_Reef.png     # バイオームマスク
    │   └── Stage01_Mask_Sand.png
    └── Generated/                    # ベイク生成物。手で編集しない
        ├── TerrainData/TD_r0_c0.asset
        ├── Tiles/Tile_r0_c0.unity
        └── Stage01_TileManifest.asset
```

`Generated/` は再ベイクで上書きされる。ただし**削除してはいけない**。
ベイカーは TerrainData とタイルシーンを作り直さず既存を開いて更新する設計で、これは Digger の掘削データがタイルシーンとTerrainDataのGUIDに紐づいているため。フォルダごと消すと掘った洞窟が全て失われる。

### ファイル名の規約

| 用途 | 名前 | 備考 |
|------|------|------|
| ハイトマップ | `<StageId>_Height.png` | 末尾が `_Height` のものは自動でR16フォーマットが適用される |
| バイオームマスク | `<StageId>_Mask_<名前>.png` | 8bitで十分 |

`Source/` 配下のテクスチャは `StageSourceTextureImporter` がインポート時に設定を強制する（Read/Write有効・sRGBオフ・無圧縮・ミップなし・Clamp）。**Inspector で変更しても再インポートで元に戻る**。手で設定する必要はない。

## 地形ソースの選択肢

ベイカーが食うのは `Source/<StageId>_Height.*` の画像1枚だけで、それをどう作ったかは関知しない。
そのため次の2つはいつでも入れ替えられる。片方に決め打つ必要はない。

| 方式 | 使うもの | 向いている場面 |
|------|---------|--------------|
| **Unity 内生成（既定）** | `StageGeneratorSettings` | 水深帯を振って5ステージを量産する。数値で意図を残したい |
| 外部ツール | WorldMachine 等 | 侵食の質を上げたい。手で描き込みたい |

### Unity 内生成

`Blue/ScriptableObject/StageGeneratorSettings` でアセットを作り、`recipe` に対象のレシピを割り当てる。
パラメータは全て**水深(m)と特徴の実サイズ(m)**で持つので、正規化値への読み替えは不要。Stage02 に転用するときは水深の数値を振り直すだけで済む。

調整は `Blue > World > Stage Preview`（またはインスペクターの `Open Preview Window`）で行う。
パラメータを触ると俯瞰図が即座に更新される。プレビューは低解像度（既定 257²）で同じ生成関数を呼んでいるため、大きな地形と斜面の角度は最終結果と一致する。一致しないのはサンプル間隔より細かいディテールだけ。

- **水深(陰影)** … 起伏の読み取り用。崩落や尾根が効いているかは色ではなく陰影で判断する
- **水深帯** … 棚・斜面・海盆の面積配分の確認用。散布と敵配置の見積もりに使う
- **等深線** … 水深の絶対値を読むためのもの。既定25m間隔

インスペクターと画面下部には岸→沖の断面図も出る。棚の幅・ドロップオフの角度・海盆の深さは、生成しなくてもここで確認できる。

生成されるもの:

- 断面（岸 → 大陸棚 → ドロップオフ → 海盆）
- リージョンごとに性格の違う起伏
- Thermal Erosion（安息角を超えた斜面が崩れて崖下に堆積する）

含まれないもの: Hydraulic Erosion（水流侵食）。細かい岩の造形はプロップの領分なので、ベイク後に Digger と散布で足す。

### マスク

ハイトマップと一緒に、スプラットと散布に使うマスクが `Source/` に書き出される。地形と同じリージョン分割・同じハイトフィールドから作るので、**マスクと地形が構成上ズレない**。

| ファイル | 内容 |
|---------|------|
| `<StageId>_Mask_Terrain.png` | R = 傾斜（0°〜90° を 0〜1）/ G = 水深（レイアウトの高さレンジで正規化） |
| `<StageId>_Mask_Region.png` | RGBA = リージョン0〜3の重み |

傾斜マスクは「急斜面は岩」を出すのに使う。リージョンだけで塗り分けると平地も崖も同じ質感になり、見た目の説得力がかなり落ちる。

`レシピのバイオームをリージョンから再構築` を押すと、レシピの `biomes` がリージョン構成から組み立てられる（マスクと channel の対応まで入る）。あとは各バイオームに `TerrainLayer` を割り当ててベイクするだけ。TerrainLayer の割り当ては名前で引き継がれるので、リージョンを触っても素材の割り当てはやり直しにならない。

1枚のマスクに入るのは4リージョンまで。5種以上使う場合は2枚目を手当てする必要がある（レシピは複数の `BiomeLayerBinding` を持てる）。なお URP の TerrainLit はレイヤー5枚目から追加の描画パスが要るので、4枚に抑えるのが無難。

#### maskRange で境界を作る

マスク値をそのまま使うと、傾斜の場合 0.1〜0.2（9〜18度）の緩斜面にも中途半端にウェイトが乗り、**全域が中間色になって切り替わって見えない**。`BiomeLayerBinding.maskRange` で「何度から何度で切り替えるか」を指定する。

- `x` 未満は0、`y` 以上は1、間は滑らかに繋ぐ
- `x` と `y` が同じなら変換しない（従来どおり）
- **`x > y` にすると反転する**

傾斜マスクの値は `角度 / 90` なので、25度なら 0.28、35度なら 0.39。砂と岩を25〜35度で切り替えるなら：

| | 砂 | 岩 |
|---|---|---|
| mask | `_Mask_Terrain` | `_Mask_Terrain` |
| channel | R | R |
| maskRange | **(0.39, 0.28)** ← 反転 | **(0.28, 0.39)** |
| weight | 1 | 1 |

ベース側にも逆向きの範囲を指定するのが要点。ベースを常時1のままにすると、岩が最大でも `weight / (1 + weight)` までしか濃くならず、純粋な岩肌にならない。

### リージョン

全域に同じノイズを掛けると、どこを泳いでも同じ景色になる。ステージをセルに分割して、セルごとに起伏の性格を変える。

セルの形はシードが決め（ジッタ付きの Voronoi、境界はノイズで歪ませてある）、**どのセルに何を置くかは人間が決める**。プレビューウィンドウの表示モードを `Regions` にして、割り当てたいリージョンを選んで地図をクリックする。

リージョンが持つのは断面からの差分だけで、水深そのものは断面プロファイルが決める。ここで水深帯まで上書きすると、水深と生態系の対応が崩れて `ScatterLayer` の `depthRange` が意味を失うため。

| パラメータ | 効果 |
|-----------|------|
| `ridgeScale` / `ridgeHeight` | 尾根の細かさと大きさ。この2つで「岩礁」と「砂地」が分かれる |
| `detailScale` / `detailHeight` | 近距離で効くざらつき |
| `depthBias` | 領域全体を深く/浅くする。窪地や高台 |
| `talusAngle` | 崩れやすさ。小さいと緩斜面、大きいと切り立つ |

`regionBlend` は境界のブレンド幅(m)。0にすると境界が段差になる。`regionWarp` は境界の歪み量で、0だと直線的な多角形として見える。

`depthBias` だけは `regionBlend × regionBiasBlendScale`（既定3倍）の幅でぼかす。高さのオフセットは起伏の振幅差より段差として目立つため。境界の斜面がきつい場合はこの倍率を上げる。

ブレンドは最近傍2セルではなく、**重みを持つ近傍セル全て**で行う。2セルだけで混ぜると「2番目に近いセル」が移動に伴って不連続に切り替わり、その切り替わり線（Voronoi 頂点から伸びる直線）で高さが飛ぶ。3セル以上を重みで混ぜれば、セルの出入りは重み0を通るので不連続が消える。

`useRegions` を切ると、`Relief` セクションの値が全域に掛かる従来の挙動に戻る。比較するとき用。

### 造作（Features）

ノイズは「全体をそれらしく荒らす」ことはできても、「ここに海丘を1つ」はできない。地図に描けるような特徴、プレイヤーが目印にする地形、Digger で彫る取っ掛かりは、造作として座標を指定して置く。

プレビューウィンドウの表示モードを `Features` にして、`造作を追加` で置き、地図上でドラッグして位置を決める（Shift+クリックでその場に追加）。座標はステージ中心を原点とした XZ(m) で、シーン上の Terrain と同じ座標系。

| 形状 | 用途 |
|------|------|
| `Dome` | 海丘・海嶺。滑らかに立ち上がる |
| `Plateau` | 台地。頂上が平らなので構造物を置きやすい |
| `Cone` | 尖った海山 |
| `Crater` | 中心が凹み縁が環状に盛り上がる。ブルーホールの土台 |

`elongation` を上げて `rotation` を振ると、円が伸びて楕円になる。`roughness` は造作の表面だけを荒らすもので、Digger で彫るときの取っ掛かりになる凹凸を作る（造作の外には漏れない）。

#### パス（折れ線）

`パス編集` を有効にして地図をクリックすると、造作に折れ線を追加できる。**2点以上あると楕円ではなく折れ線からの距離で形が決まる**ようになり、`radius` は折れ線からの片側の幅として働く。

うねった海嶺や蛇行する海溝は、造作を何個も重ねるのではなくこれ1本で作る。`height` を負にすれば同じ仕組みで海溝になるので、深いステージにもそのまま転用できる。

- 点をドラッグで移動、`Alt`+クリックで削除
- 造作本体をドラッグすると折れ線ごと平行移動する
- `パス消去` で楕円に戻る

形状（`Dome` / `Plateau` / `Cone` / `Crater`）と `roughness` はパスでもそのまま効く。`Crater` をパスに掛けると、溝の両側に堤ができた形になる。

#### 分岐

1本の折れ線は分岐を表せないので、**分岐は造作を複数置いて繋ぐ**。その際 `blend` を切り替える。

| blend | 用途 | 対象 |
|-------|------|------|
| `Add` | 加算（既定）。独立した造作を重ねる | 符号を問わない |
| `Merge` | `height` の符号から Max / Min を自動で選ぶ | **分岐を繋ぐならこれ** |
| `Max` | 高い方を採用 | `height` が正 |
| `Min` | 低い方を採用 | `height` が負 |

`Max` / `Min` を直接指定する場合は `height` の符号と対応させること。**尾根に `Min` を使うと破綻する**。造作の縁では寄与が0へ近づくため、`min(尾根, ほぼ0) ≒ 0` となって、もう一方の造作の輪郭に沿って崖ができる。海溝に `Max` を使うと同じことが逆向きに起きる。噛み合っていない場合は警告が出る。`Merge` なら符号から機械的に決まるので取り違えようがない。

`Add` のまま接合すると、重なった部分だけ高さが二重に足されて不自然に盛り上がる。Y字の海嶺を作るなら、幹と枝を別の造作にして両方 `Max` にする。

`blend` は造作ごとの設定で、パスの有無に関係なく効く。合成はモードごとにグループへ集めてから足し合わせるので、**配列の順序に依存しない**。`Max` グループ内で最も高い値、`Min` グループ内で最も低い値が採られ、`Add` はそのまま加算される。

**合成が効くのは、繋ぎたい造作を両方とも同じモードにしたときだけ。** そのモードの造作が1つしか効いていない地点では、グループの値がその寄与そのものになるので、結果は加算と変わらない。片方だけ `Max` にしても見た目は変化しない。

`featureBlendSmoothness`（既定20m）は接合部の丸め量。素の最大値は値としては連続でも傾きが不連続なので、2つの斜面が交差する線が折り目として見える。この値の範囲で滑らかに繋ぎ、接合部を少し盛り上げることで、尾根が合流したときの堆積に近い形になる。0にすると折り目が出る。

丸め量は内部で「弱い方の造作の効き具合」まで落としている。定数のまま掛けると、造作の影響範囲に入った瞬間に最大 `k/4` だけ地面が持ち上がり、輪郭が段差として見えるため。

**起伏の勾配の目安**: 半径120mで高さ80mなら約34度。ノイズ側の `ridgeScale 400` / `ridgeHeight 40` は約6度しかないので、砂丘のようにしか見えない。「山」に見せたいなら造作を使う。

`ridgeHeight` や `detailHeight` を大きくするとレイアウトの高さレンジをはみ出す。はみ出した分は平坦に潰れるが、生成ログに潰れたサンプル数が出るので、警告が出たらレンジか起伏のどちらかを調整する。

## 外部ツールを使う場合（WorldMachine）

正確な値は `Blue > World > Stage Scaffolder` の「外部ツールへの入力値」に出るので、そこからコピーする。Stage01 の場合:

| 項目 | 値 |
|------|-----|
| World Extents | 1024m × 1024m |
| 標高レンジ | 300m |
| ビルド解像度 | 1025 × 1025 |
| ハイトマップ出力 | 16bit グレースケール PNG |
| 正規化 | 0.0 → Y -280m（水深280m） / 1.0 → Y +20m |

### World Extents を合わせる理由

侵食系デバイスは世界の実寸を見て効き方を変える。ここが Unity 側とズレていると、WorldMachine のプレビューでちょうど良く見えた侵食が、焼き上がりでは細かすぎたり粗すぎたりする。

### ビルド解像度 1025 について

1025 = 4タイル × (257 - 1) + 1。隣接タイルは境界の1列を共有するため、単純な256×4にはならない。
この解像度で書き出せばベイク時に補間が挟まらず、輪郭が鈍らない。カスタム解像度を入れられない場合は1024でも実害はない（半テクセル、0.5m未満のズレ）が、**513以下には落とさないこと**。1m/texelを割ると地形のディテールがそこで頭打ちになる。
WorldMachineの無料版は出力解像度に制限があるので、1025を出せるかは事前に確認する。

### 値域を使い切る

ハイトマップの 0.0 が `minHeight`、1.0 が `maxHeight` に対応する。
出力が 0.3〜0.6 の範囲しか使っていないと、16bitのうち実質14bit分を捨てることになる。WorldMachine 側の Clamp / Normalize で 0〜1 を使い切るように調整してから書き出す。

### 侵食デバイスの使い方

海底は水中で堆積した地形なので、河川侵食（Erosion）の作る深い谷はそのままだと説明がつかない。
Thermal Erosion（崖下の崩落・talus）を主体にして、Erosion は堆積の筋を出す程度に弱く掛けるとそれらしくなる。「かつて陸地だった」設定の海域なら、Erosion を強めに掛けて沈んだ谷を作る手もある。

### ランドマークは Layout Generator で描く

Layout Generator で手描きしたシェイプは、地形への合成と**マスク出力の両方に流せる**。
同じシェイプを使えば、地形の形・バイオームのスプラット・散布物・スポーン範囲が全て一致する。ランドマークやバイオーム境界は、ノイズ任せにせずここで明示的に描く。

## 制作手順

1. `Blue > World > Stage Scaffolder` を開き、Stage Id と水深帯を入力して雛形を作成
2. `StageGeneratorSettings` アセットを作り、`recipe` に 1 で作ったレシピを割り当てる
3. `Blue > World > Stage Preview` で俯瞰図と断面を見ながら調整し、`Generate & Bake Terrain` を実行
4. タイルシーンを開いて、Terrain ブラシと Digger でランドマークと洞窟を彫る
5. シーンに `StageLoader` を置き、生成されたマニフェストを割り当てる

外部ツールを使う場合は 2〜3 の代わりに、書き出した画像を `Source/` に置いて `StageRecipe` の Inspector から `Bake Stage Terrain` を実行する。

再ベイクは何度でも可能。手でタイルシーンに足したオブジェクトも Digger の掘削も維持される。
**ただし layout を変えた再ベイクだけは別**で、TerrainData の解像度とタイル境界が変わるため掘削データは復元できない。

### ストリーミング設定

1024m / 16タイルなら全部載せきれるので、`StageLoader.mode` は `PreloadAll` を推奨。
ゲーム中にロードが走らないぶん、タイル統合によるヒッチが原理的に起きない。Terrain の LOD 接続も常に成立する。

## Stage01 の設計

### バイオーム

Terrain のスプラットレイヤーは4枚に抑える。URP の TerrainLit は5枚目から追加パスが必要になり、描画コストが跳ねるため。

| レイヤー | 用途 | マスク |
|---------|------|-------|
| 砂 | ベース | なし（常時ウェイト1） |
| 岩 | 露岩・崖・海山 | 傾斜由来のマスク |
| 礫・サンゴ片 | リーフ周辺 | Layout Generator の手描き |
| 泥 | 深部の堆積 | 水深由来のマスク |

**バイオームの数 = Terrainレイヤーの数ではない**。地形の見た目は4枚で足り、生態系の差は `ScatterLayer` と敵配置で作る。散布側のマスクは枚数制限がないので、こちらは細かく分けてよい。

| バイオーム | 水深 | 地形 | 生態・見どころ |
|-----------|------|------|--------------|
| リーフフラット | 0〜25m | 平坦、起伏小 | サンゴ、小魚の群れ、光条が届く明るい層 |
| 藻場・ケルプ | 15〜50m | 緩斜面 | 縦方向の視界遮蔽。待ち伏せ型の敵 |
| 砂地平原 | 40〜90m | 平坦 | 見通しが良い。エイ、貝、アイテム散布 |
| 岩礁・海山 | 30〜140m | 急峻、起伏大 | 洞窟入口。うろつく捕食者 |
| ドロップオフ | 90〜170m | 崖 | 垂直移動。壁面のウミウチワ |
| 泥底の深部 | 170〜250m | 平坦、低起伏 | 暗い。発光生物、シーラカンス級の大型種 |

### ランドマーク

| ランドマーク | 位置 | 役割 |
|-------------|------|------|
| ブルーホール | 浅瀬、水深0から底まで貫く縦穴 | 垂直移動の目玉。ステージ2への導線にもなる |
| 沈没船 | ドロップオフの棚、水深120m前後 | 内部探索。プレハブ配置（Digger不要） |
| 海底洞窟系 | 岩礁帯、水深60〜160m | Digger で掘る。抜け道と隠しアイテム |
| 冷水湧出帯 | 泥底、水深230m前後 | 化学合成群集。他と生態系が明確に違う一角 |

ランドマークは4つとも、まず WorldMachine の Layout Generator で土台の地形を描き、同じシェイプをマスクとして書き出す。散布とスポーンをそのマスクに紐づければ、地形とコンテンツがズレない。

## ハマりどころ

| 症状 | 原因 |
|------|------|
| 緩斜面が階段状になる | ハイトマップが8bit。16bit以上で書き出す |
| ベイクが「Read/Write Enabled が無効」で止まる | `Source/` 以外に置いている。規約のフォルダに移す |
| ステージの端が反対側の地形とつながる | ハイトマップの wrapMode が Repeat。`Source/` に置けば自動でClampになる |
| 地形がぼやける | ビルド解像度が1025未満。拡大補間されている |
| 掘った洞窟が消えた | layout を変えて再ベイクした、または `Generated/` を削除した |
| 別ステージの Terrain と繋がろうとする | `groupingId` が重複している |
